# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

GET             /hello          controllers.HomeController.hello

GET             /v1/users       controllers.UserController.list

GET             /v1/user/:id    controllers.UserController.findById(id:Int)

POST            /v1/user        controllers.UserController.add

PUT             /v1/user        controllers.UserController.update

DELETE          /v1/user/:id    controllers.UserController.delete(id:Int)